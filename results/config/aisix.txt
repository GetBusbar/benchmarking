# ── config.gen.yaml (rendered; loaded via --config) ──
resources_file: "/home/ubuntu/benchmarking/gateways/aisix/resources.gen.yaml"
proxy:
  addr: "0.0.0.0:3000"
admin:
  enabled: true
  admin_keys:
    - "aisix-admin-dummy"
observability:
  service_name: "aisix-bench"
  metrics:
    prometheus:
      enabled: true
  tracing:
    otlp:
      enabled: false

# ── resources.gen.yaml (rendered; standalone resources_file) ──
_format_version: "1"
provider_keys:
  - display_name: mock
    provider: openai
    adapter: openai
    api_base: "http://127.0.0.1:8000/v1"
    api_key: "sk-mock"
models:
  - display_name: gpt-4o-mini
    provider: openai
    model_name: gpt-4o-mini
    provider_key: mock
api_keys:
  - display_name: bench
    key_env: AISIX_BENCH_KEY
    allowed_models: ["*"]

# ── launch env (non-secret; AISIX_BENCH_KEY is the mandatory api_key plaintext, dummy on the rig) ──
AISIX_BENCH_KEY=bench-token
